{
  "name": "website-practice",
  "version": "1.0.0",
  "scripts": {
    "prepare": "husky install",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^16.1.2",
    "prettier": "^3.6.2"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,css,scss,md,json,html}": [
      "prettier --write"
    ]
  }
}
